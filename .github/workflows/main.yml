name: sample workflow

on:
  push:
    branches:
      - 'main'
  pull_request:
    types: [opened]
      
  pull_request_review:
    types: [submitted]
      
      

jobs:
  runs-on: ubuntu-latest
  steps:
    - run: echo 'Workflow for the events'

  
